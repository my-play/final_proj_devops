name: Terraform Infrastructure Setup

on:
  push:
    branches:
      - main

jobs:
  terraform:
    runs-on: self-hosted

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Install Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 16
  
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v2
      with:
        terraform_version: 1.5.0

    - name: Initialize Terraform
      run: terraform init
      working-directory: ./terraform_cd

    - name: Validate Terraform
      run: terraform validate
      working-directory: ./terraform_cd

    - name: Plan Infrastructure
      run: terraform plan -out=tfplan
      working-directory: ./terraform_cd

    - name: Apply Infrastructure
      run: terraform apply -auto-approve tfplan
      working-directory: ./terraform_cd

    - name: Get EC2 Public IP
      id: outputs
      run: |
        unset TF_LOG
        echo "Terraform Output: $(terraform output -raw ec2_public_ip)"
      working-directory: ./terraform_cd

    - name: Set EC2 Public IP Output
      run: |
        unset TF_LOG
        echo "EC2_PUBLIC_IP=$(terraform output -raw ec2_public_ip)" >> $GITHUB_ENV
        # run: echo "EC2_PUBLIC_IP=$(terraform output -raw ec2_public_ip | tr -d '\r\n')" >> $GITHUB_ENV
      working-directory: ./terraform_cd